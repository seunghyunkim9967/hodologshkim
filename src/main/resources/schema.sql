/*
DROP TABLE 'USER';

CREATE TABLE 'USER'
(
    id       BIGINT       NOT NULL,
    name     VARCHAR(20)  NOT NULL,
    email    VARCHAR(255)         ,
    password VARCHAR(100) NOT NULL,
    created_at date,
    PRIMARY KEY (id)
);*/
